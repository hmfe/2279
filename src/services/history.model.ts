export interface SearchHistory {
    id?: number;
    searchWord: string;
    date: Date;

    // constructor(id: number, searchWord: string, date: Date) {
    //     this.id = id;
    //     this.searchWord = searchWord;
    //     this.date = date;
    // }
}

